# Исправление CORS и API URL

## ✅ Что исправлено

### 1. Дефолтный API URL во фронтенде
**Файл:** `frontend/src/utils/api.ts`

- ✅ Изменён дефолтный URL с `http://localhost:8000` на `https://nanogenpro-production.up.railway.app`
- ✅ Добавлено логирование для отладки (в development режиме)

### 2. CORS конфигурация в бэкенде
**Файл:** `backend/app/main.py`

- ✅ Явно указаны разрешённые методы: `GET, POST, PUT, DELETE, OPTIONS, PATCH`
- ✅ Явно указаны разрешённые заголовки, включая `X-Telegram-Init-Data`
- ✅ Добавлен `max_age=3600` для кэширования preflight запросов
- ✅ `allow_credentials=False` (правильно для Telegram WebApp)

## 🔍 Проверка в Netlify

### Шаг 1: Убедитесь, что переменная окружения установлена

1. Зайдите в **Netlify Dashboard** → Ваш сайт → **Site settings** → **Environment variables**
2. Проверьте, что есть переменная:
   ```
   VITE_API_URL = https://nanogenpro-production.up.railway.app
   ```
3. Если её нет — добавьте и пересоберите сайт

### Шаг 2: Пересоберите сайт

После добавления/изменения переменной окружения:
1. Зайдите в **Deploys**
2. Нажмите **Trigger deploy** → **Clear cache and deploy site**

### Шаг 3: Проверка в браузере

Откройте консоль браузера (F12) и проверьте:
- Должен быть лог: `[API] Using API URL: https://nanogenpro-production.up.railway.app`
- Запросы должны идти на правильный домен

## 🧪 Тестирование CORS

### Проверка preflight запроса

Откройте в браузере:
```
https://nanogenpro-production.up.railway.app/api/user/auth
```

**Ожидаемый результат:**
- ✅ `405 Method Not Allowed` — это нормально (endpoint требует POST)
- ✅ В Response Headers должны быть:
  - `Access-Control-Allow-Origin: https://nanogenbot.netlify.app`
  - `Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS, PATCH`
  - `Access-Control-Allow-Headers: Content-Type, Authorization, X-Telegram-Init-Data, ...`

**Если видите:**
- ❌ `CORS policy: No 'Access-Control-Allow-Origin' header` — проверьте, что бэкенд перезапущен с новым кодом

## 🚀 Деплой изменений

### Бэкенд (Railway)
1. Закоммитьте изменения в `backend/app/main.py`
2. Railway автоматически перезапустит приложение
3. Проверьте логи Railway — должны быть сообщения о старте

### Фронтенд (Netlify)
1. Закоммитьте изменения в `frontend/src/utils/api.ts`
2. Netlify автоматически пересоберёт сайт
3. Убедитесь, что переменная `VITE_API_URL` установлена в Netlify

## 📝 Важные заметки

1. **Переменная окружения в Netlify** имеет приоритет над дефолтным значением в коде
2. **CORS работает только для указанных origins** — если используете другой домен, добавьте его в `allow_origins`
3. **Preflight запросы (OPTIONS)** обрабатываются автоматически FastAPI CORS middleware
4. **Telegram WebApp** не отправляет credentials, поэтому `allow_credentials=False` — правильно

## 🔧 Если проблема остаётся

1. **Очистите кэш браузера** (Ctrl+Shift+Delete)
2. **Проверьте Network tab** в DevTools:
   - Смотрите на реальный URL запроса
   - Проверьте Response Headers
   - Проверьте, что preflight OPTIONS запрос возвращает правильные заголовки
3. **Проверьте логи Railway** — должны быть запросы от фронтенда
4. **Проверьте логи Netlify Build** — должна быть переменная `VITE_API_URL`

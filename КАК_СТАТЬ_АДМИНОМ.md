# 🔐 Как стать администратором

## Быстрый способ

### 1. Узнайте ваш Telegram User ID

Откройте бота [@userinfobot](https://t.me/userinfobot) в Telegram и отправьте `/start`. Скопируйте ваш ID (например: `123456789`).

### 2. Запустите скрипт

**Вариант А: Через переменную окружения (рекомендуется)**
```bash
cd backend
export TELEGRAM_ADMIN_ID=123456789
python -m scripts.make_admin
```

**Вариант Б: Через аргумент командной строки**
```bash
cd backend
python -m scripts.make_admin 123456789
```

**Вариант В: Добавить в .env файл**
Добавьте в `backend/.env`:
```
TELEGRAM_ADMIN_ID=123456789
```
Затем просто запустите:
```bash
cd backend
python -m scripts.make_admin
```

Замените `123456789` на ваш реальный Telegram ID.

### 3. Откройте админ-панель

1. Откройте WebApp в Telegram
2. Перейдите в **DebugHub** (главный экран)
3. Нажмите на кнопку **"Админ панель"** (появится только для админов)
4. Или откройте напрямую: добавьте `?screen=admin` к URL WebApp

## Альтернативные способы

### Через SQL (если есть доступ к БД)

```sql
UPDATE users SET is_admin = true WHERE telegram_id = 123456789;
```

### Через Python консоль

```python
from app.database import AsyncSessionLocal
from app.models import User
from sqlalchemy import select
import asyncio

async def make_admin(telegram_id: int):
    async with AsyncSessionLocal() as db:
        result = await db.execute(select(User).where(User.telegram_id == telegram_id))
        user = result.scalar_one_or_none()
        if user:
            user.is_admin = True
            await db.commit()
            print(f"✅ Пользователь {telegram_id} теперь админ!")
        else:
            print(f"❌ Пользователь не найден. Сначала откройте бота или WebApp.")

asyncio.run(make_admin(123456789))
```

## Что дальше?

После назначения администратором вы сможете:

- 📊 Просматривать статистику системы
- 💳 Одобрять/отклонять платежи
- 💰 Одобрять/отклонять выводы средств
- 👤 Управлять пользователями (баланс, бан/разбан)

Подробнее: см. `backend/ADMIN_SETUP.md`
